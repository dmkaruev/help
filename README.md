
```php
clear
git init
git status
git add .
git commit -m "Commit"
git branch -M main
git branch ветки
git branch NEW
git branch -D OLD удаление ветки
git checkout NEW переход к ветке
it merge NEW совмещение веток (из OLD)
git remote add origin https://github.com/dmkaruev/help.git
git push -u origin main
git push
git pull
```
